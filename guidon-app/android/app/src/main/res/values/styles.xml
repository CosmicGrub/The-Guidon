<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Status bar: src/native.js reads the app's active theme and calls
             the Capacitor StatusBar plugin to match the bar colour to it.
             That call is a documented no-op on Android 15+ (OS-enforced
             edge-to-edge); the app's own CSS safe-area insets keep content
             clear of the bar either way. Transparent here avoids a flash of
             the wrong colour before the plugin call lands on Android 14 and
             earlier, where it still works.
             Navigation bar: nothing in the app themes it - no
             NavigationBar-capable Capacitor plugin is installed. Transparent
             is only this style's static default; it is never kept in sync
             with theme changes the way the status bar is. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Android 12+ uses the SplashScreen API: a solid brand background with the
         app icon, not a full-bleed drawable. The drawable below is the legacy
         path for Android 7-11, which minSdk 24 still includes. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brandDark</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
